use store;
describe rides
;
SELECT 
    *
FROM
    users;
SELECT 
    *
FROM
    stations;
SELECT 
    *
FROM
    rides;
use store;
SELECT DISTINCT
    user_id
FROM
    rides;
SELECT 
    user_id
FROM
    rides
WHERE
    end_station_id IS NULL;
    
 delete from rides where user_id = '5'; 
 SET SQL_SAFE_UPDATES = 0;
 SET SQL_SAFE_UPDATES = 1;
 ROLLBACK;
 SELECT *
FROM rides
WHERE user_id = 5;
DESCRIBE rides;

INSERT INTO rides (
    ride_id,
    user_id,
    start_station_id,
    end_station_id,
    start_time,
    end_time,
    distance_km
)
VALUES (
    1,
    5,
    101,
    205,
    '2024-01-01 08:00:00',
    '2024-01-01 08:20:00',
    3.5
);

select * from rides;
